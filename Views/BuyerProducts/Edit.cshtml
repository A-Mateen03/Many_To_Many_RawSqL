@model Many_To_Many_RawSqL.Models.BuyerProducts

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>BuyerProducts</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BuyerProductId" />
            <div class="form-group">
                <label asp-for="BuyerId" class="control-label"></label>
                <select asp-for="BuyerId" class="form-control" asp-items="ViewBag.BuyerId"></select>
                <span asp-validation-for="BuyerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ProductP_ID" class="control-label"></label>
                <select asp-for="ProductP_ID" class="form-control" asp-items="ViewBag.ProductP_ID"></select>
                <span asp-validation-for="ProductP_ID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
